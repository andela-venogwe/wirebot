import {
  describe,
  it,
  should
} from '../helpers';

describe('Slack Events:', () => {
  describe('Message', () => {
    describe('Bot Message And Message Changed: ', () => {
      it('should do something', () => {
        should.exist(2);
      });
    });
  });
});
